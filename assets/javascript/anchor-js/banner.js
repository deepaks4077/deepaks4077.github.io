const fs=require("fs"),pkg=require("./package.json"),filename="anchor.min.js",script=fs.readFileSync(filename),padStart=e=>("0"+e).slice(-2),dateObj=new Date,date=`${dateObj.getFullYear()}-${padStart(dateObj.getMonth()+1)}-${padStart(dateObj.getDate())}`,banner=`/**\n * AnchorJS - v${pkg.version} - ${date}\n * ${pkg.homepage}\n * Copyright (c) ${dateObj.getFullYear()} Bryan Braun; Licensed ${pkg.license}\n */\n`;"/**"!=script.slice(0,3)&&fs.writeFileSync(filename,banner+script);