<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://deepaks4077.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://deepaks4077.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-01-12T01:17:56+00:00</updated><id>https://deepaks4077.github.io/feed.xml</id><title type="html">blank</title><subtitle>This might become useful over time</subtitle><entry><title type="html">Regex and Hackerrank</title><link href="https://deepaks4077.github.io/blog/2015/Hackerrank-Regex/" rel="alternate" type="text/html" title="Regex and Hackerrank"/><published>2015-07-10T00:00:00+00:00</published><updated>2015-07-10T00:00:00+00:00</updated><id>https://deepaks4077.github.io/blog/2015/Hackerrank-Regex</id><content type="html" xml:base="https://deepaks4077.github.io/blog/2015/Hackerrank-Regex/"><![CDATA[<p>Regular expressions are one of the most versatile tools available. You can do some crazy things with regexing. Lately, I have been solving many problems on the regex subsection of <a href="https://www.hackerrank.com/domains/algorithms/regex">hackerrank</a>. The problems were fairly simple at first and I continued to code in java even though it probably would have been easier in perl or bash. But eventually the expressions got complex forcing me to abandon Java and settle with the regex alone. I used <a href="http://regexr.com/">this</a> website to check if my expressions were correct.</p> <p>I have added the expressions to a text file on github but for anyone following the blog I will add them here too:</p> <ul> <li><a href="https://www.hackerrank.com/challenges/stack-exchange-scraper">stack-exchange-scraper question</a></li> </ul> <p>Print out the question id, summary and date in the order in which they appear on a stackexchange website.</p> <ol> <li> <p>regex for id: <code class="language-plaintext highlighter-rouge"> (?:(&lt;div (.*)?id="question-summary-))([0-9]+)(?:("(.*)?&gt;)) </code> The id is the text inside capturing group #3</p> </li> <li> <p>regex for summary: <code class="language-plaintext highlighter-rouge"> (?:(&lt;h3&gt;.*?\[))(.*?)(?:\]) </code> The summary is inside capturing group #2</p> </li> <li> <p>regex for relativetime: <code class="language-plaintext highlighter-rouge"> (?:&lt;span.*?(?=class="relativetime").*?&gt;)([\w ]+?)(?:&lt;\/span&gt;) </code> The relativetime is inside capturing group #1</p> </li> </ol> <p>In Java after identifying the substrings using regex and adding them to an arraylist they can be printed out in the required format.</p> <ul> <li><a href="https://www.hackerrank.com/challenges/detect-the-email-addresses">detect-the-email-address problem</a></li> </ul> <p>Output unique email addresses from given body of text.</p> <p>Fairly simple regex:</p> <p><code class="language-plaintext highlighter-rouge">([a-zA-Z][a-zA-Z0-9]*)@[a-zA-Z]{2,}\.[a-zA-Z]{2,}</code></p> <p>This question isn’t clear because a valid email can be a fairly complicated string. RFC 822 specifies a very broad set of rules for ‘valid’ email addresses. Check out <a href="http://ex-parrot.com/~pdw/Mail-RFC822-Address.html">this</a> website for a Perl regex which is RFC 822 compliant ;)</p> <ul> <li><a href="https://www.hackerrank.com/challenges/detecting-valid-latitude-and-longitude">longitudes and latitudes</a></li> </ul> <p>Pretty complex regex :</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>\([\+\-]?(?!.*([\-\+\.,]\)))(?!\d{3,}\..*,)(?!0\d+\.,)(?!.*\.,)(?!9[1-9]\.)(?!90\.0*[1-9]0*,)(?!.*[\.\+\-].*[\.\+\-].*?,)(?!,.*[\.\+\-].*[\.\+\-].*?)(?!.*,\.)(?!.*,\d{4,}[\.]?)(?!.*,[2-9][0-9][0-9])(?!.*,1[8-9][1-9])(?!.*,180(\..*[1-9]+))(\d*\.?\d*,[\+\-]?\d*[\.]?\d*)\)
</code></pre></div></div> <p>It worked on all the sample test cases but I have not tested this completely. The strategy is to start off with the most general case; \d+(.\d+)? for numbers and then start adding negative lookaheads to customize.</p> <p>For example the regex for the range [-90,+90] could be :</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>^[\+\-]?(?!\d{3,})(?!9[1-9])(?!90\..*[1-9]+)(?!.*[\.\+\-]$)(?!.*[\+\-].*[\-\+])\d+(\.\d+)?
</code></pre></div></div>]]></content><author><name></name></author><category term="[&quot;Unix&quot;, &quot;Hackerrank&quot;]"/><summary type="html"><![CDATA[Regular expressions are one of the most versatile tools available. You can do some crazy things with regexing. Lately, I have been solving many problems on the regex subsection of hackerrank. The problems were fairly simple at first and I continued to code in java even though it probably would have been easier in perl or bash. But eventually the expressions got complex forcing me to abandon Java and settle with the regex alone. I used this website to check if my expressions were correct.]]></summary></entry><entry><title type="html">Masks And Pipes</title><link href="https://deepaks4077.github.io/blog/2015/Masks-And-Pipes/" rel="alternate" type="text/html" title="Masks And Pipes"/><published>2015-06-30T00:00:00+00:00</published><updated>2015-06-30T00:00:00+00:00</updated><id>https://deepaks4077.github.io/blog/2015/Masks-And-Pipes</id><content type="html" xml:base="https://deepaks4077.github.io/blog/2015/Masks-And-Pipes/"><![CDATA[<p>Linux is FUN. Open Source is FUN. I am looking into on an open-source project called Loris (<a href="https://github.com/aces/Loris">link</a>) and I had to inspect the installation script because of a failed install. The installation file introduced me to umasks and pipes.</p> <h4 id="bitmasks">Bitmasks</h4> <p>What is a bitmask? What are they used for? The literal meaning would suggest they are used to hide bits or the true significance/position of bits in a larger collection. While they are used in encryption (in hashing for example), their typical purpose is to help decrypt access control lists and in computer graphics.</p> <h4 id="user-mask-or-the-default-user-created-directoryfile-permission-bitmask">User Mask or The default user created directory/file permission bitmask</h4> <p>The umask is used to specify the default user created file/directory permissions and therefore the level of security for software resources.</p> <p>The maximum default file permissions are 666 and the maximum default directory permissions are 777. Why not 777 for files? According to <a href="http://www.cyberciti.biz/tips/understanding-linux-unix-umask-value-usage.html"><strong>this post</strong></a> it is because <strong><em>only binaries and scripts are executable, regular files are not</em></strong>.</p> <p>So how do you calculate the default file/directory permissions? Through the octal subtraction of the umask from file/directory maximum permissions.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Directories 777 &amp; ~umask
Files 666 &amp; ~umask 
</code></pre></div></div> <p>where ~umask is the binary complement of the umask.</p> <p>If the umask is 077 then the default directory permissions are 700 and for files its 600, which is the srictest set of permissions you can give.</p> <p>As you increase the value of the umask the default security policy becomes more loose and with 000 <strong>everyone can read, write and execute files and directories</strong> (where applicable ofcourse).</p> <p><strong>Pipes</strong></p> <p>Consider this script:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LOGPIPE=/tmp/pipe.$$
mkfifo -m 700 $LOGPIPE
trap "rm -f $LOGPIPE" EXIT
tee -a &lt;$LOGPIPE $LOGFILE &amp;
exec 1&gt;$LOGPIPE 2&gt;&amp;1
</code></pre></div></div> <p>What is the pipe.$$ file? What do the commands mkfifo, trap, tee and exec do ?</p> <ol> <li> <p>mkfifo opens a FIFO file with file extension $$ which is a special variable that is replaced by the process id. A FIFO file created by mkfifo needs to be open for writing and reading at the same time to work. So a process needs to write to the file and another process needs to read from the file. This is where the tee and exec commands come in.</p> </li> <li> <p>The trap command looks for a system signal (EXIT in this case) and executes the bash commands in the double quotes. The signal received is any signal that indicates exit like HUP and INT (hangup and interrupt respectively).</p> <p>side note: (There is no EXIT signal in Unix. The list of signals available can be viewed by executing kill -l )</p> </li> <li> <p>The tee command duplicates the input received from stdout to another file. So the output is received on stdout and also inside the file. The ‘-a’ option indicates file append. The &amp; at the end of the command makes the command run in the background so that we can continue to the next command.</p> <p>In the command above we are using &lt;$LOGPIPE to redirect the pipes stdout to the stdin of the tee command. The tee command ouputs in stdout and to the file $LOGFILE.</p> </li> <li> <p>The exec command is simple. It is redirecting stdout and stderr to the named pipe. How? 1&gt;$LOGPIPE redirects stdout to the pipe and 2&gt;&amp;1 is redirecting stderr to stdout which is already connected to the pipe.</p> </li> </ol> <p>Therefore the whole script is using a named pipe to store stderr and stdout in the logfile.</p>]]></content><author><name></name></author><category term="Unix"/><summary type="html"><![CDATA[Linux is FUN. Open Source is FUN. I am looking into on an open-source project called Loris (link) and I had to inspect the installation script because of a failed install. The installation file introduced me to umasks and pipes.]]></summary></entry><entry><title type="html">Module.exports</title><link href="https://deepaks4077.github.io/blog/2015/module.exports/" rel="alternate" type="text/html" title="Module.exports"/><published>2015-06-17T00:00:00+00:00</published><updated>2015-06-17T00:00:00+00:00</updated><id>https://deepaks4077.github.io/blog/2015/module.exports</id><content type="html" xml:base="https://deepaks4077.github.io/blog/2015/module.exports/"><![CDATA[<p>The first time I went about creating a node module I was confused how I would tell node which object to return. Creating modules in node is incredibly simple. Yet the difference between module.exports and exports is confusing. Some npm packages use the exports variable while some use module.exports to return the module object.</p> <p>So to clear things up for myself and for any reader who is just as confused as I was, here is the difference:</p> <h4 id="moduleexports">module.exports</h4> <p>When you require a module:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>var app = require('module');
</code></pre></div></div> <p>Node returns the ‘module.exports’ object. You can console.log(module) to see the exports object paramater.</p> <h4 id="exports">exports</h4> <p>‘exports’ by itself is just a global variable provided to you for convenience. Initially, the global ‘exports’ variable references the same memory location as module.exports.</p> <p>So if you assign ‘exports’ :</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>exports = //your module object
</code></pre></div></div> <p>You are changing the object <strong>exports</strong>. Javascript <em>does not pass by reference at all</em>. There is no way to pass the whole object in javascript because variables dont hold objects, they hold the values of the references to those objects. That is the value of the address of the object.</p> <p>So if I do this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>exports.a = //your module object
</code></pre></div></div> <p>Then require will work:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>var app = require('module');
console.log(app.a)//prints the contents of exports.a and not 'undefined'
</code></pre></div></div> <p>The ‘.’ operator dereferences the object and adds an ‘a’ property.</p> <h4 id="best-practice">Best practice</h4> <p>So that is the only way I can return my module using exports ‘provided I do not use module.exports anywhere in the code’. It is easy to remember the difference if you understand it.</p> <p>However, the best and most common practice right now seems to be:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>module.exports = exports = //your module object
</code></pre></div></div> <p>OR just using module.exports.</p>]]></content><author><name></name></author><category term="nodejs"/><summary type="html"><![CDATA[The first time I went about creating a node module I was confused how I would tell node which object to return. Creating modules in node is incredibly simple. Yet the difference between module.exports and exports is confusing. Some npm packages use the exports variable while some use module.exports to return the module object.]]></summary></entry></feed>